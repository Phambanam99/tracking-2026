apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tracking-platform.componentName" (dict "root" . "component" "processing") }}
  labels:
    {{- include "tracking-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: processing
spec:
  replicas: {{ .Values.processing.replicaCount }}
  selector:
    matchLabels:
      {{- include "tracking-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: processing
  template:
    metadata:
      labels:
        {{- include "tracking-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: processing
      {{- if .Values.processing.metrics.enabled }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: {{ .Values.processing.containerPort | quote }}
      {{- end }}
    spec:
      securityContext:
        {{- toYaml .Values.global.podSecurityContext | nindent 8 }}
      containers:
        - name: processing
          image: "{{ .Values.processing.image.repository }}:{{ .Values.processing.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.processing.containerPort }}
          {{- if or .Values.config.create .Values.config.existingConfigMap .Values.secrets.create .Values.secrets.existingSecret }}
          envFrom:
            {{- if or .Values.config.create .Values.config.existingConfigMap }}
            - configMapRef:
                name: {{ include "tracking-platform.configMapName" . }}
            {{- end }}
            {{- if or .Values.secrets.create .Values.secrets.existingSecret }}
            - secretRef:
                name: {{ include "tracking-platform.secretName" . }}
            {{- end }}
          {{- end }}
          {{- with .Values.processing.env }}
          env:
            {{- range $key, $value := . }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          {{- end }}
          {{- if .Values.global.probes.startup.enabled }}
          startupProbe:
            {{- include "tracking-platform.httpProbe" .Values.global.probes.startup | nindent 12 }}
          {{- end }}
          livenessProbe:
            {{- include "tracking-platform.httpProbe" .Values.global.probes.liveness | nindent 12 }}
          readinessProbe:
            {{- include "tracking-platform.httpProbe" .Values.global.probes.readiness | nindent 12 }}
          resources:
            {{- toYaml .Values.processing.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tracking-platform.componentName" (dict "root" . "component" "processing") }}
  labels:
    {{- include "tracking-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: processing
spec:
  type: ClusterIP
  selector:
    {{- include "tracking-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: processing
  ports:
    - name: http
      port: {{ .Values.processing.service.port }}
      targetPort: http
