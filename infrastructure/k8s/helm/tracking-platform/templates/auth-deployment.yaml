apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tracking-platform.componentName" (dict "root" . "component" "auth") }}
  labels:
    {{- include "tracking-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: auth
spec:
  replicas: {{ .Values.auth.replicaCount }}
  selector:
    matchLabels:
      {{- include "tracking-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: auth
  template:
    metadata:
      labels:
        {{- include "tracking-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: auth
      {{- if .Values.auth.metrics.enabled }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/actuator/prometheus"
        prometheus.io/port: {{ .Values.auth.containerPort | quote }}
      {{- end }}
    spec:
      securityContext:
        {{- toYaml .Values.global.podSecurityContext | nindent 8 }}
      containers:
        - name: auth
          image: "{{ .Values.auth.image.repository }}:{{ .Values.auth.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.auth.containerPort }}
          {{- if or .Values.config.create .Values.config.existingConfigMap .Values.secrets.create .Values.secrets.existingSecret }}
          envFrom:
            {{- if or .Values.config.create .Values.config.existingConfigMap }}
            - configMapRef:
                name: {{ include "tracking-platform.configMapName" . }}
            {{- end }}
            {{- if or .Values.secrets.create .Values.secrets.existingSecret }}
            - secretRef:
                name: {{ include "tracking-platform.secretName" . }}
            {{- end }}
          {{- end }}
          {{- with .Values.auth.env }}
          env:
            {{- range $key, $value := . }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
          resources:
            {{- toYaml .Values.auth.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tracking-platform.componentName" (dict "root" . "component" "auth") }}
  labels:
    {{- include "tracking-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: auth
spec:
  type: ClusterIP
  selector:
    {{- include "tracking-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: auth
  ports:
    - name: http
      port: {{ .Values.auth.service.port }}
      targetPort: http
