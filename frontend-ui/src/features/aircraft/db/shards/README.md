Seed shard data for the frontend aircraft DB loader.

This is intentionally tiny and synthetic. It exists so the lazy shard-loading
path is exercised in development and tests until a real tar1090-db export is
added to the repo.
